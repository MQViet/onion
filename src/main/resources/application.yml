
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onion
    username: postgres
    password: pg112233
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true